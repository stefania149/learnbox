name: Publicare pe GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# O singură publicare odată; o publicare în curs nu se anulează.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  constructie:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Export static
        env:
          # Site de proiect: totul stă sub /<nume-repo>/.
          NEXT_PUBLIC_BASE_PATH: /${{ github.event.repository.name }}
          NEXT_PUBLIC_VERSIUNE: ${{ github.sha }}
        run: npm run build

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  publicare:
    needs: constructie
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.publicare.outputs.page_url }}
    steps:
      - id: publicare
        uses: actions/deploy-pages@v4
